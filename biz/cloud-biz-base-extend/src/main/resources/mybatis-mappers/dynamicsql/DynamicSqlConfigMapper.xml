<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.midea.cloud.srm.base.dynamicsql.mapper.DynamicSqlConfigMapper">
	<select id="getSqlData" parameterType="String" resultType="java.util.Map">
		${value}
	</select>
    <select id="getSqlDataCount"  parameterType="String" resultType="int">
        ${value}
    </select>
</mapper>
